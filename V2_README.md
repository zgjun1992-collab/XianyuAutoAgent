# 闲鱼餐饮卡券 AI 客服 V2

## 第一次使用

1. 安装并打开“闲鱼卡券 AI 客服”。
2. 在“闲鱼工作台”内登录闲鱼。软件会自动同步登录状态，不需要打开 F12 或手工复制 Cookie。
3. 进入“AI与连接”，填写模型 API Key、接口地址和模型名称。API Key 使用 Windows DPAPI 加密保存在当前电脑。
4. 进入“商品知识库”，填写闲鱼商品 ID 和名称，然后直接粘贴任意格式的商品资料。
5. 点击“保存原始资料”，再点击“AI重新归纳”。原始资料始终是最高优先级，AI摘要不会覆盖它。
6. 进入“适用门店”，导入 Excel 并绑定商品。
7. 进入“测试中心”，先验证真实买家问法。测试不会向买家发送消息。
8. 确认无误后，在右上角启动客服。

## 回答规则

- 信息优先级：用户原始资料 > AI归纳摘要 > 闲鱼商品页面。
- 门店查询只使用当前商品绑定的门店表；模糊命中时回复“xxx门店可用”，查不到时回复该门店不可用，多个相近结果会要求买家补充具体门店。
- 时间判断使用北京时间，并区分工作日、周末和早餐/午餐/下午茶/晚餐/夜间。
- 只回答和推荐买家正在咨询的当前商品，禁止推荐其他商品。
- 降价、退款、赔偿、延期、换码等承诺由本地规则拦截并进入人工审核。
- 连续消息先聚合 2.5 秒；收到更新消息时旧草稿自动作废。
- 真实券码不会传给语言模型。

## 数据位置

正式安装后，商品资料、门店、审核记录和加密配置保存在当前 Windows 用户的应用数据目录，不随安装包分发。

## 开发与构建

- 运行测试：`.venv\Scripts\python.exe -m unittest discover -s tests -v`
- 开发启动：`powershell -ExecutionPolicy Bypass -File .\start-v2-dev.ps1`
- 构建安装包：`powershell -ExecutionPolicy Bypass -File .\build-v2.ps1`
