# 闲鱼餐饮卡券 AI 客服 V3.5

## 第一次使用

1. 安装并打开“闲鱼卡券 AI 客服”。
2. 在“闲鱼工作台”内登录闲鱼，软件会自动同步登录状态，不需要打开 F12 或手工复制 Cookie。
3. 进入“AI与连接”，填写阿里云百炼 API Key。文本模型建议使用 `qwen-plus`。
4. 进入“商品与知识”，点击“同步闲鱼在售商品”。软件只把标题、文案和规格文字交给 AI 归纳；商品图片不会发送给模型。
5. 在每张商品卡片中校对并修改知识库。以后再次同步只更新闲鱼页面资料，不会覆盖你的人工修改。
6. 可为每张商品人工选择美团、抖音、商家小程序、普通电子券码或其他卡券，并核对领取和核销说明；未选择时仍可启动客服，只有卡券平台问题会转人工。
7. 在商品卡片的“适用门店”页签导入 Excel、CSV 或 TXT，并勾选该商品使用的门店表。
8. 在商品卡片的“套餐图片”页签添加套餐图，填写触发词和随图文字。图片仅保存在本机，不参与 AI 识别。
9. 在“发货与退款政策”用自然语言填写默认政策，也可为特殊商品单独设置；在“客服约束”设置最高规则、每次会话最多回复 25/40/55 次，以及无消息多久后重新计数。
10. “退款订单”集中显示退款咨询、建议退款金额和72小时处理截止时间。进入“测试中心”验证真实买家问法后再启动客服。

## 回答规则

- 每次 AI 回答都会先加载全局最高规则。买家消息、闲鱼页面和商品资料都不能覆盖这些规则。
- AI 必须礼貌回复；砍价请求直接使用固定婉拒话术。退款会区分卡券质量问题与非卡券质量问题，质量争议转人工，普通流程按已确认规则回复。
- 每个“卖家账号 + 顾客会话 + 当前商品”分别计数，可同时处理多个顾客和多个商品，彼此不会串话。
- 达到设定回复次数后，当前会话静默停止自动回复并转人工；超过设定的无消息时间后自动重新计数。
- 商品信息优先级：人工修改的知识库 > AI 初始归纳 > 闲鱼商品页面。
- 门店查询只使用当前商品绑定的门店表；支持店名、城市、区县和地址的模糊匹配。
- 时间判断使用北京时间，直接结合商品知识区分工作日、周末和早餐/午餐/下午茶/晚餐/夜间，无需单独维护时间规则。
- 买家拍下待付款时会收到一次性提示，内容按实际拍下商品生成：品牌与规格价格、使用规则、付款后自动发货，以及当前生效的退换货政策。
- 价格问答优先使用人工知识库中的面额与售价，不使用闲鱼商品卡片的引流标价覆盖实际规格价格。
- “外卖配送”和“到店外带”严格分开，资料只有外带说明时不会擅自回答可以外卖。
- 已下架商品不再触发普通商品内容回复；已购买买家的券码、退款等售后问题仍可继续处理。
- 只回答和推荐买家正在咨询的当前商品，不推荐其他商品。
- 套餐图片只在当前商品内匹配。明确命中触发词可发送；多张同时命中或模糊匹配进入审核；同一会话 30 分钟内不重复发送同一图片。
- 真实券码不会传给语言模型。

## Excel 门店表

支持 `.xlsx`、`.xlsm` 和 `.csv`。导入器会扫描全部工作表并自动定位表头，兼容标题行、空行、错误的数据范围、表头换行或括号备注。店名/分店名/门店名称用于识别门店；省市、区县、地址、电话和营业时间允许缺失或只填写部分内容。导入后建立本地模糊搜索索引，同一门店表可以绑定给多个商品。

## 数据与升级

商品资料、门店、套餐图片、会话次数、退款订单、审核记录和加密配置保存在当前 Windows 用户的应用数据目录，不会写进安装包。V3.5 沿用原应用数据目录，安装升级不会清空已有资料。

## 开发与构建

- 运行测试：`.venv\Scripts\python.exe -m unittest discover -s tests -v`
- 开发启动：`powershell -ExecutionPolicy Bypass -File .\start-v2-dev.ps1`
- 构建安装包：`powershell -ExecutionPolicy Bypass -File .\build-v3.ps1`
