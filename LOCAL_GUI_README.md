# 闲鱼餐饮卡券 AI 客服（本地可视化版）

## 启动

双击 `launch-gui.cmd`。软件会读取项目内 `.env` 中已经配置的 API Key 和闲鱼 Cookie。

## 首次使用

1. 在“账号与AI”中核对模型与闲鱼 Cookie，点击“测试AI连接”。
2. 在“商品资料”中新建商品，商品 ID 必须与闲鱼会话中的商品 ID 一致。
3. 填写有效期、适用门店、退款规则等资料并保存。
4. 在“客服约束”中保持“人工审核”模式。
5. 点击“启动客服”，等待右上角变为“已连接”。
6. 买家发来消息后，在“待审核回复”中检查、编辑并发送。

## 信息优先级

本地商品资料 > 闲鱼商品页面资料 > 通用客服提示词。发生冲突时只采用本地资料；资料缺失时要求 AI 明确表示暂未确认。

## 安全约束

降价、退款、赔付、延期、换码、补发、门店范围保证等高风险事项会进入人工审核。规则审查独立于大模型，不能仅靠提示词绕过。

## 数据位置

- 敏感配置：`.env`（Git 已忽略）
- 商品资料、规则和审计：`data/app.db`（Git 已忽略）
- 对话上下文：`data/chat_history.db`（Git 已忽略）

不要提交 `.env`、数据库或任何真实券码。
