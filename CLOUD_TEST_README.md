# 云端授权测试版

这是从 V3.5 独立复制出来的测试工程。旧项目和真实用户数据库不会被该工程修改。

## 当前能力

- 后台人工创建客户；
- 开通3天体验、30天月卡、365天年卡；
- 续费从当前有效期末尾顺延；
- 一个套餐默认绑定一台设备和一个闲鱼账号；
- 冻结客户、撤销设备和审计日志；
- 客户端账号登录和设备自动绑定；
- 在线授权检查和48小时断网宽限；
- 未授权时禁止启动自动客服；
- 到期不会删除本地商品、知识、门店或历史数据。

## 本机联调

测试工程没有复制 `.venv` 和 `node_modules`。可将旧项目中的依赖目录复制或建立目录联接，再运行：

```powershell
$env:LICENSE_ADMIN_KEY = "替换成至少24位的随机管理员密钥"
.\.venv\Scripts\python.exe -m cloud_license.server --host 127.0.0.1 --port 8787 --db .\work\cloud-license-test.db
```

打开 `http://127.0.0.1:8787/admin`，输入相同管理员密钥，创建客户并开通套餐。

客户端“账号与授权”页面测试地址保持 `http://127.0.0.1:8787`。

## 正式部署前必须完成

- 将授权服务放到 HTTPS 域名之后；
- 管理页面增加独立管理员账号、MFA 和 CSRF 防护；
- 将 SQLite 换为 PostgreSQL；
- 管理密钥放入服务器 Secret，不写进仓库或安装包；
- 配置防火墙、限流、备份、监控和操作日志留存；
- 使用代码签名证书签署 Windows 安装包。

当前管理密钥模式只用于朋友圈私售的封闭测试，不能直接裸露到公网。

## 回退

测试工程基线提交：`e5b90a00c07d7be64e342981f933b0ff62d495b2`。

旧版本项目仍位于原目录，未执行任何修改。测试工程需要回到初始状态时，先备份测试数据，再从该提交创建新的工作副本；不要在旧项目中执行清理命令。
